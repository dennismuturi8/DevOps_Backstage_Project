apiVersion: v1
kind: Service
metadata:
  name: my-service
spec:
  type: NodePort
  ports:
    - port: 443       # The port the service is exposed on
      targetPort: 7007  # The port your application is listening on
  selector:
    app: backstage